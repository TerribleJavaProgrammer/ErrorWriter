#include "core/input.hpp"

InputEvent pollInput() {
    // Implement a function to poll input
}

bool isSpecialKey(InputEvent e) {
    // Implement a function that returns whether an input event is a special key
}

char inputToChar(InputEvent e) {
    // Implement a function that turns an input event into a char
}
